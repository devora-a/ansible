- name: demo on loops
  hosts: local
  connection: local
  tasks:
  - name: print names 
    debug:
      msg: "HI {{ item }}" # item is reserived special variable
    loop:
    - ARAVIND
    - DARLING
    - MAMA  